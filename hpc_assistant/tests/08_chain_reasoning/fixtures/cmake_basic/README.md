# Sample HPC Project

This repository demonstrates a minimal CMake-based workflow.

## Build Instructions
1. `cmake -S . -B build`
2. `cmake --build build -j 8`

## Run Instructions
- Submit `sbatch scripts/run_basic.sh`
