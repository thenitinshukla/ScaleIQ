[
  {
    "id": "tool-sequence-basic-01",
    "scenario": "tool_sequence",
    "fixture": "cmake_basic",
    "task": "Plan and virtually execute repository intake, environment inspection, configuration, compilation, and sbatch planning for this project. The existing script scripts/run_basic.sh must be used for submission.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "Repository freshly cloned into /workspace/projects/sample; sbatch must call scripts/run_basic.sh"
    },
    "expectations": {
      "must_include_commands": ["git status", "module list", "cmake", "sbatch"],
      "allow_failures": false
    }
  },
  {
    "id": "tool-sequence-basic-02",
    "scenario": "tool_sequence",
    "fixture": "cmake_basic",
    "task": "Repeat the full repo→env→build→sbatch workflow, citing specific module considerations for the provided environment.",
    "context": {
      "cluster": "Leonardo dcgp",
      "modules": ["gcc/11.2", "openmpi/4.1"],
      "notes": "User provided README summarises build." 
    },
    "expectations": {
      "must_include_commands": ["git status", "module list", "cmake -S", "cmake --build"],
      "allow_failures": false
    }
  },
  {
    "id": "tool-sequence-basic-03",
    "scenario": "tool_sequence",
    "fixture": "cmake_basic",
    "task": "Perform the standard workflow and ensure the launch plan references scripts/run_basic.sh in commands and REPORT.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/22.11"],
      "notes": "scripts/run_basic.sh must be used in the final sbatch command"
    },
    "expectations": {
      "must_include_commands": ["git status", "module list", "cmake -S", "cmake --build", "scripts/run_basic.sh"],
      "allow_failures": false
    }
  },

  {
    "id": "partial-failure-01",
    "scenario": "partial_failure",
    "fixture": "cmake_failure",
    "task": "Execute the typical workflow. If any step fails, diagnose it, propose a fix, and retry until the plan is consistent.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "First cmake call should report missing CUDA toolkit; expect module load recovery"
    },
    "expectations": {
      "failure_trigger": "cmake",
      "recovery_keywords": ["module load", "spack install", "retry"],
      "must_include_commands": ["git status", "module list", "cmake", "cmake --build"],
      "allow_failures": true
    }
  },
  {
    "id": "partial-failure-02",
    "scenario": "partial_failure",
    "fixture": "cmake_failure",
    "task": "Carry out the workflow, handling build failures by recommending module remedies before retrying.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "Expect explicit mention of module nvhpc/23.3 as remedy"
    },
    "expectations": {
      "failure_trigger": "cmake --build",
      "recovery_keywords": ["module load nvhpc", "set(CMAKE_CUDA_COMPILER"],
      "must_include_commands": ["git status", "module load", "cmake"],
      "allow_failures": true
    }
  },
  {
    "id": "partial-failure-03",
    "scenario": "partial_failure",
    "fixture": "cmake_failure",
    "task": "Perform the workflow; if GPU requirements are unmet, advise on partition/module adjustments and continue.",
    "context": {
      "cluster": "Leonardo dcgp",
      "modules": ["gcc/11.2"],
      "notes": "Expect suggestion to switch partition or load CUDA module"
    },
    "expectations": {
      "failure_trigger": "cmake",
      "recovery_keywords": ["module load nvhpc/23.3", "module load cuda", "use booster"],
      "must_include_commands": ["git status", "module", "cmake"],
      "allow_failures": true
    }
  },

  {
    "id": "ambiguity-01",
    "scenario": "ambiguity",
    "fixture": "hybrid_build",
    "task": "Choose the safest build tooling for this hybrid repository, justify the decision, and complete the sbatch planning. Ensure the job is submitted via sbatch.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "CMake preferred over Make"
    },
    "expectations": {
      "preferred_command": "cmake",
      "disallowed_keywords": ["make all"],
      "must_include_commands": ["git status", "module list", "cmake", "cmake --build"],
      "allow_failures": false
    }
  },
  {
    "id": "ambiguity-02",
    "scenario": "ambiguity",
    "fixture": "hybrid_build",
    "task": "Analyse both build options and recommend the most reliable one for this environment, then finish the workflow. The sequence must include sbatch submission.",
    "context": {
      "cluster": "Leonardo dcgp",
      "modules": ["gcc/11.2"],
      "notes": "Make allowed only if justified but prefer cmake"
    },
    "expectations": {
      "preferred_command": "cmake",
      "disallowed_keywords": [],
      "must_include_commands": ["git status", "module list", "cmake", "sbatch"],
      "allow_failures": false
    }
  },
  {
    "id": "ambiguity-03",
    "scenario": "ambiguity",
    "fixture": "hybrid_build",
    "task": "Discuss the merits of Make vs CMake here, pick the safest option, and produce the normal PLAN/COMMANDS/REPORT.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "Expect explanation if Make is suggested"
    },
    "expectations": {
      "preferred_command": "cmake",
      "disallowed_keywords": ["make"],
      "must_include_commands": ["git status", "module list", "cmake"],
      "allow_failures": false
    }
  },

  {
    "id": "memory-load-01",
    "scenario": "memory_load",
    "fixture": "cmake_large_context",
    "task": "Summarise the salient points from the supplied log, then run the full repo→env→build→sbatch reasoning sequence.",
    "context": {
      "cluster": "Leonardo booster",
      "modules": ["nvhpc/23.3"],
      "notes": "Include large log excerpt",
      "log_file": "logs/build_output.log"
    },
    "expectations": {
      "must_include_commands": ["git status", "module list", "cmake -S", "cmake --build", "sbatch"],
      "allow_failures": false
    }
  },
  {
    "id": "memory-load-02",
    "scenario": "memory_load",
    "fixture": "cmake_large_context",
    "task": "Read the large log excerpt, highlight warnings worth attention, and proceed with the standard workflow.",
    "context": {
      "cluster": "Leonardo dcgp",
      "modules": ["gcc/12.1"],
      "notes": "Log emphasises warnings; expect summarised reasoning",
      "log_file": "logs/build_output.log"
    },
    "expectations": {
      "must_include_commands": ["git status", "module list", "cmake", "cmake --build"],
      "allow_failures": false
    }
  },
  {
    "id": "leonardo-dryrun-01",
    "scenario": "dry_run",
    "fixture": "leonardo_dryrun",
    "task": "Prepare the end-to-end workflow on Leonardo while keeping every scheduler command in dry-run mode. Confirm module availability and validate the batch script without dispatching work to compute nodes.",
    "context": {
      "cluster": "Leonardo login",
      "modules": ["nvhpc/23.3", "openmpi/4.1"],
      "notes": "Login node has internet access; GPU nodes do not. Use scripts/run_leonardo.sh in the final submission."
    },
    "expectations": {
      "must_include_commands": ["git status", "module spider", "module list", "srun --test-only", "sbatch --test-only"],
      "dry_run_only": true,
      "allow_failures": false
    }
  },
  {
    "id": "leonardo-resource-mismatch-01",
    "scenario": "partial_failure",
    "fixture": "leonardo_dryrun",
    "task": "The initial sbatch attempt uses the wrong partition. Diagnose the failure, choose the booster partition for GPUs, and retry in dry-run mode until the submission validates.",
    "context": {
      "cluster": "Leonardo login",
      "modules": ["nvhpc/22.11"],
      "notes": "Only the booster partition has the requested GPUs. Remember that compute nodes lack internet access."
    },
    "expectations": {
      "failure_trigger": "--partition compute",
      "failure_message": "Partition compute does not expose GPUs. Switch to partition booster before retrying.",
      "recovery_keywords": ["--partition booster", "--partition=booster"],
      "must_include_commands": ["git status", "module list", "module spider", "sbatch --test-only"],
      "dry_run_only": true,
      "allow_failures": true
    }
  }
]
