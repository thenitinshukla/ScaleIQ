# Tinygrad Fixture

This fixture mirrors a freshly cloned tinygrad repository. Inspect the project tree (`ls`, `cat README.md`) to understand available scripts, then follow best practices from the system prompt to configure environments, run smoke tests, and stage sbatch dry-runs.
