[
  {
    "id": "tinygrad-setup",
    "scenario": "tinygrad_install",
    "task": "Set up the tinygrad repository from https://github.com/geohot/tinygrad.git on the Leonardo login node. Use dry-run commands for any scheduler interaction, verify dependencies, and prepare an sbatch plan for the booster partition.",
    "context": {
      "cluster": "Leonardo login",
      "modules": ["nvhpc/22.11"],
      "notes": "Planner must load nvhpc/23.3 before GPU work. Login node has internet; compute nodes do not."
    },
    "expectations": {
      "must_include_commands": [
        "module spider tinygrad",
        "module load nvhpc/23.3",
        "git clone https://github.com/geohot/tinygrad.git",
        "pip install -e .",
        "python3 test/test_ops.py --quick",
        "sbatch --test-only"
      ],
      "preferred_command": "srun --test-only",
      "dry_run_only": true,
      "allow_failures": false
    }
  },
  {
    "id": "unsloth-finetune",
    "scenario": "unsloth_pipeline",
    "task": "Prepare the unsloth finetuning environment from https://github.com/unslothai/unsloth.git. Ensure dependencies are installed, dataset directory is staged, and an sbatch dry-run is generated for booster.",
    "context": {
      "cluster": "Leonardo login",
      "modules": ["nvhpc/23.3"],
      "notes": "Datasets live under /leonardo/home/userexternal/<uid>/datasets."
    },
    "expectations": {
      "must_include_commands": [
        "module spider unsloth",
        "git clone https://github.com/unslothai/unsloth.git",
        "pip install -r requirements.txt",
        "pip install -e .",
        "mkdir -p datasets",
        "sbatch --test-only"
      ],
      "dry_run_only": true,
      "allow_failures": false
    }
  }
]
