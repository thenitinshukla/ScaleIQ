# Repository layout checklist

- `runs/` exists with private permissions and contains a `.keep` placeholder.
- `tests/` contains at least the suites scheduled by the Master Test Plan.
- `utils/` stores only promoted APIs and specification files.
- The root README documents the updated structure.
- No temporary files or build artifacts are present at the repository root.
