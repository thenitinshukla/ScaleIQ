# Make project layout

```
Makefile
src/
  main.cpp
  solver.cpp
include/
  solver.hpp
```

- `make all` builds `solver` executable and `libsolver.a`.
- `make clean` removes `build/` directory and artifacts.
