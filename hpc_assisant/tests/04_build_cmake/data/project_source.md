# Project source preparation

- Use the repository cloned in suite 02 (`hpc-toy` example) or provide a tarball located at `data/hpc-toy.tar.gz`.
- Ensure the source tree includes a `CMakeLists.txt` with targets `solver` and `solver_tests`.
- Copy the source into the run workspace under `runs/<timestamp>/workspace/` before configuring.
