# Clone logging expectations

- Record start and end timestamps.
- Capture command executed (mask tokens if present).
- Store exit code and commit SHA.
- Include path to scratch workspace used for the clone.
